Registrar principal en el ioc(x)

Revisar get_features_modules para que no gestione los modulos
y delegarlos al contexto de app(X)

config de aplicación(X)
	Cargar middelwares	
	desarrollo y production
	Estudiar esto

	# Opción 1: desde __main__
    import __main__
    if hasattr(__main__, '__file__') and __main__.__file__:
        main_dir = os.path.dirname(os.path.abspath(__main__.__file__))
        features_path_fs = features_path.replace(".", os.sep)
        potential_path = os.path.join(main_dir, features_path_fs)
        if os.path.exists(potential_path):
            base_path = potential_path    
	intertar cambiar sys.args[0] para extraer el path del main


Ver si hay forma de particionar multiples OpenApi en el mismo microservicio,
o mejor tener los schemas separados por tags1(X)

Estudio de Roles

	Resolver si el el rol puede hacer la accion creo que eso implica que el rol
	acumula todos los scopes

caches de servidor

logger