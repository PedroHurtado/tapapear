1. Convertir los metodos de los repositorios 
        get_by_<name>(name:str):...
        get_by_<email>(email:str):...
        get_by_<phone>(phone:str):...
remplazarlos por una llamada
    a este metodos
      con esta firma
      async def find_by_field(
        self, field: str, value: Any, limit: Optional[int] = None
    ) -> list[T]:
Es decir el <name>, <email> or <phone>
    pasarian a llamar a
    find_by_field("name",name)
    find_by_field("name",email)
    find_by_field("name",phone)


2. Ver la mejor manera de los campos de indice unico, donde meterlos.

3. Ver codigo repetido de pydantic
    quitar None al hacer model_dump
    ver donde poner id,refence,collection
    implementar __get__ en firestore repositori events

4. Estudiar el proceso que lee eventos sin procesar los procesa
    los borra de una collection y los guarda en otra collection
5. Despues de estudiar algolia ver si interesa extraer el data 
    de cada evento de cara a modificaciones mas eficientes
    y que generen menos trafico

